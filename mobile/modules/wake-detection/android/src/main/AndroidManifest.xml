<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <!-- Android 14+ demands a declared type. Waking the user is not one of the
       named categories, so specialUse is the correct bucket; the subtype string
       below is what Play review reads. -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <application>
    <!-- Fully qualified deliberately: a relative ".Name" in a *library*
         manifest is resolved against the application's package at merge time,
         not this module's, and would point at a class that does not exist. -->
    <service
      android:name="expo.modules.wakedetection.WakeDetectionService"
      android:exported="false"
      android:foregroundServiceType="specialUse">
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="Detects the user waking up from device motion, to deliver the waking-adhkar reminder the moment they pick the phone up." />
    </service>

    <receiver
      android:name="expo.modules.wakedetection.WakeBootReceiver"
      android:enabled="true"
      android:exported="false">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
      </intent-filter>
    </receiver>

    <receiver
      android:name="expo.modules.wakedetection.WakeDismissReceiver"
      android:enabled="true"
      android:exported="false" />
  </application>
</manifest>
