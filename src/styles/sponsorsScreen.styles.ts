import { StyleSheet } from 'react-native';

/** Figma sponsor list — node 18272:3330. Body padding 16h / 20v, 12px gap. */
export const sponsorsScreenStyles = StyleSheet.create({
  content: { paddingHorizontal: 16, paddingVertical: 20, flexGrow: 1 },
  separator: { height: 12 },
});
