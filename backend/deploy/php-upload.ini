; Quranic Clinic — PHP-FPM overrides applied by deploy.sh.
; Report screenshots can exceed the stock 2M cap, which silently drops the
; upload before Laravel sees it. Keep these >= the API's 5 MB image validation.
upload_max_filesize = 8M
post_max_size = 10M
