# Credentials never enter this repo at any level (a Google client secret
# leaked once from an .env — see backend/.githooks/pre-commit, armed via:
#   git config core.hooksPath backend/.githooks
# which must be re-run after every fresh clone).
.env
.env.*
!.env.example
